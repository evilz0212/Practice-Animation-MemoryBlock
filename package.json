{
    "name": "ex-js-memoryblock",
    "version": "0.0.0",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "git-pages": "vite build && git checkout -b temp && git add -f dist && git commit --allow-empty -m 'Initial dist subtree commit' && git subtree split --rejoin --prefix dist -b gh-pages && git push -f origin gh-pages && git checkout master && git branch -D gh-pages temp"
    },
    "dependencies": {
        "vue": "^3.0.0"
    },
    "devDependencies": {
        "@vue/compiler-sfc": "^3.0.0",
        "pug": "^3.0.0",
        "sass": "^1.26.11",
        "vite": "^1.0.0-rc.4"
    }
}
